# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master
- task: AzureFileCopy@4
  inputs:
    SourcePath: '/*'
    azureSubscription: 'Gold Sponsorship (80b79fb2-fb7d-497d-9158-960c9d900153)'
    Destination: 'AzureBlob'
    storage: 'azdevopsstorage1'
    ContainerName: '$web'